#ifndef FUNCTION_LIKE_MACRO_H
#define FUNCTION_LIKE_MACRO_H

/**
 * File: 3-function_like_macro.h
 * Auth: wr*
 * Desc: A header file that defines a function-like macro
 * ABS(x) which computes the absolute value of a number x.
 * @x: character to compute
 */

#define ABS(x) ((x) < 0 ? (-x) : (x))

#endif
