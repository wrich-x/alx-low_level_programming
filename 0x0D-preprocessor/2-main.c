#include <stdio.h>

/**
 * File: 2-main.c
 * Auth: wr*
 * main - prints the name of the file of program
 * it was compiled from folowed by a new line.
 * Return: 0 shows succesful compilation
 */
int main(void)
{
	printf("Compiled from file: %s\n", __FILE__);

	return (0);
}
