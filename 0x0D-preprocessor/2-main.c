#include <stdio.h>

/**
 * main - prints the name of the file of program
 * it was compiled from folowed by a new line.
 * Return: 0 Success
 */
int main(void)
{
	printf("%s\n", __FILE__);

	return (0);
}
