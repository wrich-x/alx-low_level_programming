#include <stdlib.h>
/**
 * main - entry point
 * wrx
 * Description: assign a random number to the variable n
 * Return: 0
 */
int main(void)
{
	for (int x = 'A'; x <= 'Z'; x++)
	{
		x = tolower(x);
		putchar("%c ", x)\n;
	}
	return (0);
}
